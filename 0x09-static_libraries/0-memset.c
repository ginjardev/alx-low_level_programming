#include "main.h"

/**
 * _memset - memory set
 * @s: string parameter
 * @b: char parameter
 * @n: int parameter
 * Return: string
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}

#include "main.h"

/**
 * _memset - fill memory with constant byte
 * @s: destination string
 * @b: character b
 * @n: int n
 *
 * Return: s
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
